#pragma once
#include "vector.h"
double* Unit(double* v,int tam);
/**
* @file unit.h
* @brief Esta función calcula un vector unitario dado el vector original. Si se ingresa un vector cero, el vector se establece en cero.
* 
* @param v vector
* @param tam tamaño de v
*
* @return Devuelve el vector unitario
*
*/